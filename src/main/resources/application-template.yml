spring:
  application:
    name: demo
  main:
    headless: false
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your-api-key-here}
      base-url: https://api.openai.com
      chat:
        options:
          model: gpt-4o
          temperature: 0.1
